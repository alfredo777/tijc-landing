<nav id="navbar" class="fixed top-0 w-full z-50 transition-all duration-300">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-20">
            <div class="flex items-center gap-3">
              <a href="/">  <img src="/images/logo.png?v={{timestamp}}" alt="Logo" class="h-12 w-auto"></a>
            </div>
            
            <div class="hidden lg:flex items-center gap-8">
                {{#each nav}}
                <a href="/inscripcion" class="text-sm font-medium hover:text-dorado transition-colors">{{this.texto}}</a>
                {{/each}}
                <a href="/inscripcion" class="bg-dorado text-rosa-dark px-6 py-2 font-bold uppercase text-sm hover:bg-white transition-colors">
                    Inscríbete
                </a>
            </div>

            <button id="menuBtn" class="lg:hidden text-2xl">
                <i class="fas fa-bars"></i>
            </button>
        </div>
    </div>

    <div id="mobileMenu" class="hidden lg:hidden bg-rosa-dark">
        <div class="px-4 py-4 space-y-3">
            {{#each nav}}
            <a href="/inscripcion" class="block py-2 hover:text-dorado">{{this.texto}}</a>
            {{/each}}
            <a href="/inscripcion" class="block bg-dorado text-rosa-dark text-center py-3 font-bold mt-4">Inscríbete</a>
        </div>
    </div>
</nav>